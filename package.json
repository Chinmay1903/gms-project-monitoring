{
  "name": "gms-project-manager",
  "private": true,
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  },
  "scripts": {
    "heroku-postbuild": "npm --prefix frontend ci --include=dev || npm --prefix frontend install --no-audit --no-fund --production=false && npm --prefix frontend run build && rm -rf backend/backend/frontend_build && mkdir -p backend/backend/frontend_build && cp -r  frontend/build/* backend/backend/frontend_build"
  }
}
