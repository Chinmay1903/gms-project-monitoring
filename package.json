{
  "name": "gms-project-manager",
  "private": true,
  "scripts": {
    "heroku-postbuild": "cd frontend && npm ci && npm run build && rm -rf ../backend/static && mkdir -p ../backend/static && cp -r dist/* ../backend/static"
  }
}
