{
  "name": "gms-project-manager",
  "private": true,
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  },
  "scripts": {
    "heroku-postbuild": "cd frontend && npm ci && npm run build && rm -rf ../backend/static && mkdir -p ../backend/static && cp -r build/* ../backend/static"
  }
}
